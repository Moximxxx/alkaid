# INC-2026-0506-003: Stream 参数设置但 API 返回非流式响应

- **日期**：2026-05-06
- **类型**：调试耗时
- **关联约束**：无
- **现象**：请求已正确设置 `stream: true`，但 API 返回完整 JSON 响应而非 SSE 流式格式
- **根因**：minimax API 响应格式与标准 OpenAI 兼容格式存在差异
- **修复**：同时支持 `delta.content`、`text`、`message.content` 三个字段，取第一个有值的字段
- **复盘结论**：NO_ACTION（问题已正确修复，经验教训记录即可）
